# [v1.3.0] 뉴슐랭 가이드 (Newchelin Guide) 기능명세서

## 1. 개요
- **목적**: 사내 점심/회식 식당 의사결정 지원 및 정보 공유.
- **플랫폼**: Google Workspace (Google Sheets + Google Apps Script).
- **접근 권한**:
    - **식당 정보**: 로그인한 전사 구성원 누구나 추가/수정/삭제 가능. (v1.3.0부터 어드민 제한 해제)
    - **리뷰 수정**: 어드민 여부와 무관하게 **본인이 작성한 리뷰만** 가능.
    - **리뷰 삭제**: 일반 사용자는 본인 리뷰만, **어드민은 모든 리뷰** 삭제 가능.
    - 식당/리뷰 삭제는 모두 `enabled=false` 처리(Soft Delete)이므로 시트에서 복구 가능.
    - 어드민 전용 기능은 리뷰 삭제와 마이페이지의 **관리자 설정**(관리자 이메일/게스트/기본 제외 인원)으로 한정.
    - 권한 검증은 UI 노출 제어와 별개로 서버(`ReviewService`)에서 접속자 이메일(`Session.getActiveUser()`) 기준으로 재확인.

## 2. 기술 스택
- **FE**: Vue.js 3 (Composition API), Tailwind CSS, Phosphor Icons.
- **BE**: Google Apps Script (GAS).
    - **필수 추가 서비스 (Advanced Services)**: `Google Sheets API`, `Admin Directory API`
- **DB**: Google Sheets.

## 3. 아키텍쳐 & 데이터베이스
### 3.1. 프론트엔드 전략
- **SPA 구조**: `List`, `Wheel`, `MyPage` 탭 기반 화면 전환.
- **상태 관리**:
    - **Optimistic UI**: 서버 응답 대기 없이 로컬 상태 선행 업데이트로 빠른 UX 제공 (식당 정보, 리뷰, 관리자 설정 공통 적용).
    - **Validation**: 관리자 설정 시 이메일 형식 검사 및 전체 사용자 목록 대조를 통한 오타 방지 로직 적용.
    - **Caching Strategy**:
        - `restaurants`: 전체 식당 리스트 캐싱.
        - `menuMap`: `{ restaurantId: [menus...] }` 구조로 메모리 캐싱하여 상세 모달 진입 시 로딩 지연 제거.
        - `userLikes`, `myReviewedRestaurantIds`: Set 구조로 필터링 성능 최적화.
    - **Loading UX**:
        - 초기 로딩 시 위트 있는 문구("사무실 근처 맛집 정보...", "빅데이터 수영 중..." 등)가 랜덤으로 롤링되어 지루함 완화.

### 3.2. 백엔드 (GAS) 로직
- **MenuService**:
    - **Overwrite Strategy**: 메뉴 업데이트 시 '식당ID'로 검색된 기존 행들을 순서대로 덮어쓰기 → 남는 행은 Soft Delete (`enabled=false`) → 부족한 행은 Insert.
    - **Avg Price**: `가격 > 0`인 메뉴들의 평균값 계산 (0원 서비스 메뉴 제외) 후 반환.
- **RestaurantService**:
    - 식당 저장 시 `MenuService`를 호출하여 메뉴 데이터를 저장하고, 반환된 평균 가격을 식당 정보에 반영.
    - **Rich Text Extraction**: `location` 컬럼의 셀에 삽입된 Google Maps 링크(Rich Text)를 추출하여 프론트엔드 `mapUrl`로 제공.
- **Data Serialization**:
    - Sheet API의 객체 반환 값(예: RichTextValue, Date)을 JSON 직렬화 가능한 문자열/숫자로 변환하여 클라이언트에 전달.
- **Admin & Guest Management**:
    - 관리자 권한, 게스트 목록, 조 편성 제외자 명단을 코드(Config)가 아닌 DB(Google Sheets)에서 직접 관리하여 유연성 확보.
    - 모든 데이터는 `id`(UUID) 및 `created_at` 타임스탬프를 포함하여 관리.

### 3.3. 데이터베이스 (Google Sheets)
- **restaurant**:
    - `id`, `name`, `category`, `tag`, `location` (Rich Text Link 지원), `mapUrl`
    - `signature_menu`: (Text) 리스트 뷰 표시용 캐시 데이터.
    - `price`: (Number) 메뉴 평균 가격 캐시.
    - `rate`, `like_count`, `review_count`, `enabled`, `created_at`, `updated_at`
- **menu**:
    - `id`, `restaurant_id`, `name`, `price`, `is_signature`, `enabled`, `created_at`, `updated_at`
- **review**: `id`, `restaurant_id`, `rate`, `comment`, `user_name`, `user_email`, `enabled`, `created_at`, `updated_at`
- **like**: `id`, `restaurant_id`, `user_email`, `enabled`, `created_at`
- **admin**: `id`, `email`, `created_at` (관리자 이메일 목록)
- **guest**: `id`, `name`, `email`, `department`, `created_at` (게스트 사용자 목록)
- **excluded**: `id`, `email`, `created_at` (기본 제외 인원 목록)

---

## 4. 상세 기능 정의

### 4.1. 공통 헤더 (Header)
- **레이아웃 (v1.3.0 개편)**:
    - **좌측**: 로고 → 탭 메뉴(식당 리스트 / 오늘 뭐먹지? / 누구랑 먹지?)를 로고 바로 오른쪽에 좌측 정렬.
    - **우측**: 프로필 아이콘 우측 정렬.
    - **모바일(< md)**: 1행에 로고와 프로필, 2행에 탭 메뉴를 중앙 정렬로 래핑.
- **버전 표기**:
    - **위치**: 로고 텍스트 블록 안, '룰루랄라' 오른쪽 / '뉴슐랭 가이드' 위쪽 줄에 baseline 정렬.
    - **스타일**: `text-[11px] text-gray-400`, '룰루랄라'와 `gap-3`(12px) 간격을 두어 "룰루랄라 뉴슐랭 가이드"와 버전이 별개로 읽히게 처리.
    - **소스**: `javascript.html`의 `APP_VERSION` 상수. 릴리즈 시 본 문서 제목의 버전과 함께 갱신.
- **프로필**: 사용자 등급(랭킹), 작성 리뷰 수, 좋아요 수 표시.
- **내 정보**: 클릭 시 **마이페이지** 탭으로 이동.
- **랭킹 시스템 (Tiers)**:
    - **아이언** (0개), **브론즈** (1~9개), **실버** (10~29개), **골드** (30~49개), **플래티넘** (50~69개), **다이아몬드** (70~99개), **블랙 다이아몬드** (100개+).
    - 각 등급별 고유 아이콘 및 컬러 적용.

### 4.2. 식당 리스트 (Tab: List)
- **검색 및 필터**:
    - **Exclusive Filter**: 찜한 매장 / 리뷰 남긴 매장 / 리뷰 대기 매장(미작성 매장).
    - **Price Filter**: 최소~최대 가격 범위 (슬라이더).
    - **기본 필터**: 상호명(띄어쓰기 무시)/태그/카테고리 검색.
- **정렬 (Sorting)**:
    - **Default**: **좋아요(찜) 순**으로 초기 정렬.
    - **Desktop**: 테이블 헤더 클릭 시 정렬 토글 (이름, 카테고리, 가격, 평점, 리뷰, 좋아요).
    - **Mobile**: 드롭다운 선택 + 오름/내림차순 토글 버튼.
- **UI 표시**:
    - **Desktop (Grid)**: 테이블 형태, 호버 시 툴팁으로 대표 메뉴 3개 요약 표시.
    - **Mobile (Card)**: 카드형 UI, 좋아요 파티클 애니메이션.

### 4.3. 식당 상세/추가/수정 (Modal)
- **진입 경로**: 리스트 탭 우측 하단 플로팅 버튼(+)으로 추가 모드 진입. 버튼은 로그인 사용자에게만 노출.
- **뷰 모드 (View)**:
    - **관리 기능**: 로그인한 사용자(`canEditRestaurant`)라면 누구나 상단 **케밥 메뉴(⋮)**를 통해 [수정하기/삭제하기] 접근 가능. (v1.3.0부터 어드민 제한 해제)
    - **메뉴 표시**: `is_signature=true`인 메뉴 최상단 배치 및 강조 아이콘 표시.
    - **지도**: Google Maps 임베딩 (위치 정보 없을 시 대체 UI).
- **수정 모드 (Edit)**:
    - **메뉴 관리**: 동적 리스트(추가/삭제), 가격 숫자만 입력 제한, 대표메뉴(★) 토글.
    - **중복 체크**: 상호명 입력 시 실시간으로 유사 매장명 검색 및 경고 노출.
    - **저장 로직**: 메뉴 리스트 Clean(빈 이름 제거) → 서버 전송 → 캐시 업데이트.
- **리뷰 영역**:
    - **수정 버튼(✏️)**: 본인이 작성한 리뷰에만 노출 (`canEditReview`).
    - **삭제 버튼(🗑)**: 본인 리뷰 또는 어드민에게 노출 (`canDeleteReview`).
    - **읽기 전용 모드(readonly)**: 룰렛/조 편성 결과에서 진입한 경우 수정/삭제 버튼 모두 미노출.

### 4.4. 오늘 뭐먹지? (Tab: Wheel)
- **명칭**: 기존 '랜덤 뽑기'에서 '오늘 뭐먹지?'로 변경.
- **룰렛**: 필터링된 식당 리스트를 대상으로 랜덤 추첨.
- **애니메이션**: 가속/감속 물리 효과, 핀(Pin) 튕김 애니메이션 적용.
- **편의 기능**: '현재 필터 결과 모두 추가', '카테고리별 추가', '찜한 매장 추가' 지원.

### 4.5. 마이페이지 (Tab: MyPage)
- **대시보드**: 현재 랭크, 다음 랭크까지 남은 리뷰 수, 진행률 프로그레스 바.
- **탭 구성**:
    1.  **내 정보**: 프로필 요약 및 랭크 상세 정보.
    2.  **나의 리뷰**: 내가 작성한 리뷰 및 해당 식당 카드 (최신순 정렬).
    3.  **찜 리스트**: 내가 좋아요한 식당 카드 리스트.
    4.  **관리자 설정** (관리자 전용):
        - **공통**: 모든 설정 변경은 **Optimistic UI**를 적용하여 대기 시간 없이 즉시 반영되며, 실패 시에만 자동 롤백 및 알림 제공.
        - **관리자 이메일 설정**: 어드민 권한을 가질 이메일 목록 관리. (추가 시 컨펌 생략)
        - **게스트 관리**: '누구랑 먹지?' 조 편성에 참여할 외부 인원 추가/삭제 기능.
        - **기본 제외 인원 관리**: '누구랑 먹지?' 조 편성 시 기본적으로 체크 해제될 사용자의 이메일 설정. (추가 시 컨펌 생략)

### 4.6. 누구랑 먹지? (Tab: LunchMate)
- **개요**: 점심 식사 파티(조)를 랜덤으로 구성하는 기능.
- **데이터 소스**: GWS(Google Workspace) 전체 인원 리스트 (이름, 이메일, 프로필사진, 부서 정보) 및 DB에 등록된 게스트 목록.
- **UI 구성**:
    - **인원 선택**: 부서별(Department)로 그룹핑하여 표시. 각 그룹별 '전체 선택/해제' 가능.
    - **커스텀 추가**: 각 부서 하단 입력창을 통해 외부 인원이나 임시 참가자를 직접 텍스트로 추가 가능 (IME 중복 입력 방지 적용).
    - **인원별 시각적 구분**:
        - **외부 게스트 (`isGuest`)**: 이름 옆에 회색 `GUEST` 배지 표시 및 점선 테두리(`border-dashed`) 스타일 적용. 소속 정보는 실제 등록된 부서명 연동.
        - **임시 참가자 (`isCustom`)**: 이름 옆에 보라색 별표(*) 표시. 소속 정보에 '임시 참가자' 문구 노출.
    - **애니메이션**: '조 편성하기' 클릭 시 화면 중앙에 해당 인원의 이름표(카드)가 등장하여 결과 섹션으로 날아가는 시각적 효과 제공 (이미지 프리로딩 최적화 적용).
- **조 편성 옵션**:
    - **편성 기준**: 조당 인원(기본: 4명) 또는 조 개수 기준 선택.
    - **섞기 모드**:
        - **전체 랜덤 섞기**: 구분 없이 무작위 셔플.
        - **개발 좋아 (Dev Mix, 기본값)**: 개발팀(Dev/Solution Development 포함)과 비개발팀을 분리하여 셔플 후, 조별로 균등하게 지그재그 배정.
- **기본 설정**:
    - 초기 진입 시 특정 인원(예: 휴직자, 공용 계정 등)은 **관리자 설정에서 등록된 제외자 명단**을 기반으로 선택 해제 상태로 시작.
- **결과 공유**: 편성 결과를 텍스트로 클립보드에 복사 기능 제공.
